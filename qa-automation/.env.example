QA_LIVELABS_USERNAME=
QA_LIVELABS_PASSWORD=

# Optional: use a saved Playwright storage state instead of signing in through the UI.
# QA_AUTH_TARGET_URL=https://livelabs.oracle.com/ords/r/dbpm/<private-workshop-url>
# QA_STORAGE_STATE=.auth/livelabs-storage-state.json

# Optional: route Playwright browser and link probes through a corporate proxy.
# QA_PROXY_SERVER=http://proxy.example.com:80
# QA_PROXY_BYPASS=localhost,127.0.0.1
