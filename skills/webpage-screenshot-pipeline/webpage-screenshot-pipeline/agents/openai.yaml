interface:
  display_name: "Webpage Screenshot Pipeline"
  short_description: "Capture deterministic webpage screenshots"
  default_prompt: "Use $webpage-screenshot-pipeline to capture desktop and mobile screenshots for a URL into output/screenshots/. Run the package-local Playwright setup first if needed."
