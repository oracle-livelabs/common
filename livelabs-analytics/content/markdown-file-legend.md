# Markdown File Legend

## Green

The markdown file evidence is aligned. The acknowledgement date matches the Git update month.

## Yellow

The markdown file needs review but is not automatically a failure. The acknowledgement date is within the current 12-month review threshold, which still needs manager validation.

## Red

The markdown file needs priority attention. The acknowledgement date is more than 12 months behind the Git update evidence.
